<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
           <button type="button" class="btn btn-primary" disabled>Mua</button>
            <input type="number"
                min="1",
                max="100",
                step="1"
            >
            <p class="card-text">Giá bán: {{course.gia}} đ</p>
        </div>
        <div class="col-lg-9">
           <h2>{{course.name}}</h2>
            {{!-- <img class="card-img-top" src="imgs/{{course.img}}" alt="Card image cap">
             --}}
              <img class="card-img-top" src="../img/{{course.img}}" alt="Card image cap">
            
        </div>
    </div>
</div>